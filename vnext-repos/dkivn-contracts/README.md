# dkivn-contracts

Single source of truth for DKIVN VNext schemas and cross-process contracts.

This directory is staged as an export-ready repository root because the current GitHub connector cannot create repositories. It must remain independently buildable and versioned.
