import test from 'node:test';import assert from 'node:assert/strict';import crypto from 'node:crypto';import {buildV2JsonRequest,buildSignedQuery} from '../src/v2-transport.js';
test('V2 JSON order signs queryString + exact compact JSON body',()=>{const r=buildV2JsonRequest({path:'/api/v2/futures/order',apiKey:'key12345',secret:'secret123',timestamp:123,recvWindow:5000,body:{symbol:'BTC-SWAP-USDT',side:'BUY',positionSide:'LONG',type:'LIMIT',newClientOrderId:'DKV49M_x',valueQuantity:'10.00',price:'79000.1',timeInForce:'POST_ONLY'}});assert.equal(r.headers['Content-Type'],'application/json');assert.equal(r.body.includes('\n'),false);assert.match(r.body,/"symbol":"BTC-SWAP-USDT"/);assert.match(r.body,/"timeInForce":"POST_ONLY"/);const q='timestamp=123&recvWindow=5000',sig=crypto.createHmac('sha256','secret123').update(q+r.body).digest('hex');assert.equal(r.url,`https://api.toobit.com/api/v2/futures/order?${q}&signature=${sig}`);});
test('signed query carries symbol/category and query-only signature',()=>{const r=buildSignedQuery({path:'/api/v2/futures/open-orders',apiKey:'key12345',secret:'secret123',timestamp:123,params:{symbol:'BTC-SWAP-USDT',category:'USDT'}});assert.equal(r.method,'GET');assert.match(r.url,/symbol=BTC-SWAP-USDT/);assert.match(r.url,/category=USDT/);});
